# Phase 0 Task 7 Report

## Summary

Added a checksum-first, decrypt-second Local restore drill with a uniquely named
and port-isolated Supabase workdir. The drill restores roles, schema, data, and
Storage in order, compares migrations/row counts/Storage checksums, records RTO,
then stops and removes only the validated temporary target.

## Scope

- Phase 0 plan Task 7 and restore safety controls.
- Synthetic encrypted backup only. No hosted endpoint, project ref, B2 object,
  Production data, or everyday `colorplay` Local database was read or changed.

## Files

- `scripts/backup/restore-local.sh`
- `scripts/backup/compare-restored-inventory.mjs`
- `scripts/backup/create-backup.sh`
- `docs/deployment/runbooks/restore.md`
- `tests/contracts/phase0-restore.test.ts`
- `package.json`
- `.superpowers/sdd/progress.md`

## Verification

- TDD RED: 8/8 target/checksum/workdir/restore cases failed before the restore
  implementation existed.
- GREEN: backup/restore contracts passed 2 files / 13 tests; the real isolated
  restore test completed in about 48 seconds.
- Exact plan commands created an encrypted synthetic set and printed
  `LOCAL_RESTORE_VERIFIED`; the RTO report was `pass` for `isolated-local`.
- Checksum corruption returned `RESTORE_CHECKSUM_MISMATCH` before the age marker
  existed. URLs, project refs, `/`, home, and repository root were rejected.
- `shellcheck` passed for both backup shell scripts; `pnpm lint`, `pnpm
  typecheck`, scoped Prettier, and `git diff --check` passed.
- Local review found no Critical or Important issue in the destructive-target
  boundary. Cleanup names and stops only `colorplay_restore_*`, never `--all`.

## Risk

This proves the synthetic logical-dump format and isolation machinery, not a
hosted Production disaster restore. Real Auth/system-schema compatibility,
large Storage sets, hosted Candidate timing, and RTO ≤8h require the later
owner-gated Candidate rehearsal. The synthetic artifacts were moved out of the
worktree to `/tmp/colorplay-phase0-synthetic-backup-20260806-040113` after the
gate and can be regenerated by the documented command.
